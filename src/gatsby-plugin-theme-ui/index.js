const primary = `#e8304a`
const heading = `#e8304a`
const background = `#1f2022`
const text = `#ffffff`

const theme = {
  colors: {
    text,
    primary,
    heading,
    background,
  },
}

export default theme
